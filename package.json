{
  "name": "trove",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "build:sidecar": "pnpm --filter trove-sidecar build",
    "dev": "pnpm build:sidecar && vite",
    "build": "pnpm build:sidecar && tsc && vite build",
    "test": "vitest",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-fs": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vitest": "^4.0.18"
  }
}
